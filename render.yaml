services:
  - type: web
    name: feedback-app
    runtime: python
    plan: free
    buildCommand: |
      pip install -r requirements.txt
      python manage.py migrate
      python manage.py collectstatic --no-input
    startCommand: |
      # CREATE SUPERUSER (GUARANTEED TO WORK)
      python manage.py shell -c "
      import os
      from django.contrib.auth.models import User
      
      username = 'shiva'
      email = 'shiva@gmail.com'
      password = 'shiva@2004'  # CHANGE THIS TO YOUR PASSWORD
      
      print('🔄 Creating superuser...')
      
      if not User.objects.filter(username=username).exists():
          user = User.objects.create_user(
              username=username,
              email=email,
              password=password,
              is_superuser=True,
              is_staff=True,
              is_active=True
          )
          print(f'✅ Superuser {username} created!')
          print(f'📧 Email: {email}')
          print(f'🔑 Password: {password}')
      else:
          print('✅ Superuser already exists')
      "
      gunicorn main.wsgi:application
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: feedback-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "False"
      - key: ALLOWED_HOSTS
        value: feedback-app.onrender.com
      - key: PYTHON_VERSION
        value: "3.11.0"
    healthCheckPath: /
    autoDeploy: true

databases:
  - name: feedback-db
    databaseName: feedback-db
    plan: free
